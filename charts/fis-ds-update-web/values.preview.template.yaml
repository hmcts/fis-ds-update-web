nodejs:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}

  keyvaults:
    fis-kv:
      secrets:
        - redis-access-key
        - AppInsightsInstrumentationKey
        - redis-access-key-v6
